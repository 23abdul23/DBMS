Put club room images in this folder and update `frontend/constants/sacCatalog.js` to point each room to its final asset.

The current SAC screen uses the shared `aegisIdLogo_bg.png` file as a temporary placeholder image for every club room.
